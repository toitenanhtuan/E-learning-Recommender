# Bỏ qua các dependencies, chúng sẽ được cài đặt lại trong container
node_modules

# Bỏ qua thư mục build trên máy local (nếu có)
build

# Bỏ qua các file log
npm-debug.log
yarn-debug.log
yarn-error.log

# Bỏ qua các file cấu hình của môi trường local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Bỏ qua thư mục .vscode (nếu có)
.vscode